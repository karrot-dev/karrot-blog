dependencies:
  post:
    - bundle exec jekyll build
test:
  override:
    - "true"
general:
  artifacts:
    - _site
deployment:
  master:
    branch: master
    owner: yunity
    commands:
      - ./deploy.sh
